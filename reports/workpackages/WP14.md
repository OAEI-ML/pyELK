# WP14 — encoded structural compiler and native session handoff

Status: repository-owned hidden implementation checkpoint complete; capability and release
acceptance open
Prepared: 2026-07-20
Capability claim: none
Checkpoint revision: `886f6a3dad537704a3408973602f58e3bdbec671`

## Outcome

pyELK now contains the complete repository-owned encoded structural ingestion path behind an
empty capability ledger. It validates the frozen structural-columns v1 envelope and all eleven
read-only columns, compiles the full scalar-supported ELK constructor surface directly into
Rust-owned IR, constructs the permanent native session without private-IR serialization, and
retains every required owner until close. Direct, decoded/indexed, mmap, selected overlay,
base-plus-delta overlay, direct composite, recursive composite, and nested anonymous-scope
composition paths have exact scalar compiler/result comparisons.

Unsupported constructs remain transactional whole-axiom decisions with the exact scalar feature
counts; there is no per-row callback or semantic fallback after encoded handoff. Scalar Python and
scalar-wire Rust remain complete when capability negotiation returns no handoff. Public
`Reasoner.diagnostics()` now publishes a sorted immutable, path-safe compiler handoff containing
the canonical digest, compiler/IR schema versions, implementation/native ABI, consumer compile
time, encoded-view publication time where applicable, scalar materialization rows, and the exact
encoded ownership/copy/segment ledger. Recursive composite staging reports four bytes per
resolved root posting and 64 bytes per anonymous-scope digest pair.

This is not capability promotion or WP14 acceptance. pyELK's native
`encoded_view_schemas()` and the candidate pyowl-core view capability both remain empty. The only
ordinary benchmark producer is explicitly labelled `experimental-scalar-fallback`, and its
results are not gate-eligible.

## Revision ledger

The checkpoint is the contiguous 60-commit ancestry range
`7edbb075b4395adf6f300314cd695711fcaf3057^..886f6a3dad537704a3408973602f58e3bdbec671`.
Important review boundaries are:

| Revision | Checkpoint boundary |
|---|---|
| `7edbb075b4395adf6f300314cd695711fcaf3057` | normative encoded structural compiler specification |
| `9b6765689166d7b1ab49427ab8f0744602a138c2` | frozen hidden public-core handoff seam |
| `adee801` through `0a57e8f` | descriptor/schema, borrowed-buffer, constructor-role, reference, and canonical-order validation |
| `8358bea` through `b587be2` | scalar-equivalent compiler surface, transactional unsupported policy, overlays/composites/scopes, and frozen-corpus parity |
| `5cbccf6bd323f2a2252ffb271ac1fa49924da872` | hidden encoded `Reasoner` session activation |
| `987fe973faa817b6415f721a63ddec473e9b155d` | fail-closed release-gate harness |
| `616db77` through `c0346bc` | fuzz/lifecycle coverage, phase timings, bounded fixture closure, duplicate-scan removal, and detached exporters |
| `2628952c343452f237836b135e99e902ef825dd8` | authoritative public compiler handoff diagnostics and staging-copy accounting |
| `886f6a3dad537704a3408973602f58e3bdbec671` | ingestion-path/backend diagnostic binding and scalar encoded-field rejection |

The abbreviated ranges above are Git ancestry ranges, not omitted acceptance claims. The command
`git log --reverse 7edbb07^..886f6a3` enumerates the exact series.

## Deliverable ledger

| WP14 deliverable | Repository checkpoint | Evidence / remaining gate |
|---|---|---|
| Schema negotiation and defensive validation | implemented, hidden | Exact descriptor digest, schema/model/scope, fingerprint, segment graph, column shape/type/bounds/order, resource-limit, and hostile-object checks exist. Capability ledgers stay empty. |
| Direct Rust ELK compiler | implemented | The frozen constructor-role ledger is exhaustive; every scalar-supported expression/axiom path has digest/count/result parity and every unsupported family follows the scalar policy. |
| Permanent native session and ownership | implemented | No private-IR serialization; direct/indexed owners, overlays, composite members, recursive scopes, close/fork/thread/panic/interpreter shutdown, and exporter lifetimes are covered. |
| Complete scalar fallbacks | implemented | Python remains compiler-free; Rust scalar-wire remains complete; capability absence selects scalar before consumption. |
| Differential and hostile evidence | implemented locally | Frozen/generated/W3C/native matrices, hash/worker determinism, corrupted descriptors, limits, fuzz smoke, direct/mmap/overlay/composite, and public facade integration pass. Final installed advertised-core matrix remains open. |
| Timing/copy/materialization diagnostics | implemented | Native phases, publication/consumer timing, compiler digest/schema, buffer ownership, postings, staging copies, private-IR bytes, and scalar-row materialization are bounded scalar diagnostics. |
| End-to-end release performance | harness implemented; evidence blocked | The gate requires a real advertised producer, labelled full corpus, two warm-ups/five samples, private biomedical hashes/digests, RSS, and same-machine Java evidence. Experimental fallback runs cannot pass. |
| Packaging/release/consumer handoff | partial checkpoint | Pure wheel, local Rust, Exact, and OAEI checks pass. Final tier-one native wheels, sanitizer/platform/SBOM/dependency audits, advertised core range, and final downstream pins remain open. |

## Verification evidence

The following gates were run from the repository checkpoint on 2026-07-20. The original complete
suite used pyOWLCore `34b9e841dbda3ad0062d9d578008084de754142e`, Exact-OM
`ebff7bed20f8ae0762a79cc86d7743f643748aac`, and OAEI-Bio-ML-eval
`fcebff14a99e4ea928a8c7b8bf7a4b6d142f970f`. Subsequent downstream protocol checks used
pyOWLCore `47ec85f`, Exact-OM `680112e`, and OAEI-Bio-ML-eval `2f91a3a`; they tighten evidence
validation without promoting the still-unadvertised core/pyELK capability. Documentation-only
worktree edits in dependencies are not release pins.

```text
PYTHONDONTWRITEBYTECODE=1 PYTHONPATH=src:../pyOWLCore/src \
  .venv/bin/python -m pytest -q -p no:cacheprovider
1302 passed in 96.56s

.venv/bin/ruff format --check .
.venv/bin/ruff check .
PYTHONPATH=src:../pyOWLCore/src .venv/bin/python -m mypy
136 files already formatted; Ruff passed; mypy checked 90 files with no issues

PYTHONPATH=src:../pyOWLCore/src .venv/bin/python .venv/bin/lint-imports
3 contracts kept, 0 broken

cargo fmt --all -- --check
cargo clippy --locked --workspace --all-targets --all-features -- -D warnings
cargo test --locked --workspace --all-features
Rust 1.97.1 format/clippy passed; 56 pyelk-core tests passed

PYTHONDONTWRITEBYTECODE=1 PYTHONPATH=src:../pyOWLCore/src \
  .venv/bin/python tools/oracle.py verify
passed

PYTHONDONTWRITEBYTECODE=1 PYTHONPATH=src:../pyOWLCore/src \
  .venv/bin/python tests/parity/runner.py --backend python --workers 1
124/124

PYTHONDONTWRITEBYTECODE=1 PYTHONPATH=src:../pyOWLCore/src \
  .venv/bin/python tests/parity/runner.py --backend rust --workers 1 --workers 0
124/124 at workers=1; 124/124 at workers=0

Exact-OM focused provenance/reasoner/scale/shared-stack handoff suite
59 passed

OAEI-Bio-ML-eval full suite after its isolated scalar/encoded performance protocol
208 passed, 6 optional skips, 69 subtests passed
```

OAEI revision `2f91a3a` adds an executable three-repetition/one-warm-up minimum, fresh-process RSS,
fixed-identity, 25% wall, 20% RSS, and complete zero-forbidden-counter gate. Its generated smoke
workload is explicitly diagnostic-only and its actual scalar-fallback run failed encoded
acceptance as designed; no timing from that smoke is a release result. Exact revision `680112e`
likewise requires complete zero-work, zero direct-copy, released-GIL, and exact core-call evidence
from every selected encoded consumer.

At `2628952c343452f237836b135e99e902ef825dd8`, the universal wheel built in forced-pure mode and
passed `tools/check_artifact.py --expect pure-wheel`; its archive SHA-256 was
`f5ade3d5d2426a8687e275faf417743a571032184aafa95d7158a60a9da50cc3`. The subsequent
`886f6a3` change only tightened facade diagnostic validation and passed the complete Python and
downstream suites above; this older artifact hash is recorded as evidence for that exact revision,
not presented as a wheel built from the final checkpoint.

## Acceptance blockers

1. **Advertised core contract — pyowl-core WP17/WP18.** The tested core reports
   `CoreCapabilities.encoded_view_schemas == {}`. pyowl-core must freeze and advertise the exact
   structural-columns schema, descriptor, API/adapter/package range, direct/mmap/overlay/composite
   producer behavior, and canonical fixture digests. pyELK must then pin and test that released or
   reviewed candidate range. Neither package may infer capability from private bindings.
2. **Gate-eligible performance evidence — labelled runner/corpus owner.** Run the real advertised
   producer through direct, mmap, overlay, and composite workloads plus the full generated and
   hash-pinned biomedical corpus with two warm-ups and five measured samples. The report must pass
   exact compiler/result parity, zero parser/resolver/wire/scalar materialization and base
   flattening, at least 2x encoded view-to-result geometric-mean speedup, a boundary fraction below
   5%, and the 10% per-workload time/RSS guardrails.
3. **Same-machine Java/release baseline — release owner.** Attach the pinned ELK 0.6.0 comparison
   from the same machine and satisfy the existing Java-relative and prior-release thresholds. A
   smoke report, unrelated hardware, or the experimental scalar producer cannot substitute.
4. **Installed native artifact matrix — release CI.** Rebuild and test tier-one ABI3 wheels across
   supported platforms/interpreters with the advertised core, forced Python and Rust selections,
   pure-wheel/sdist fallback, sanitizers/fuzz where prescribed, dependency/linkage, SBOM/licence,
   no-Java, and Python-source equality audits.
5. **Final consumer revisions — Exact/OAEI owners.** Repeat the shared-view/composite identity and
   bounded handoff checks against clean pinned Exact-OM and OAEI revisions after core promotion;
   record those revisions without adding a runtime dependency cycle.

Until all five blockers close, documentation and backend reports must call encoded ingestion an
experimental hidden implementation, `encoded_view_schemas` must remain empty, and `auto` must not
select it from an advertised capability.

## Handoff

Dependent work may rely on scalar semantics, the compiler digest/schema meaning, exact public
diagnostic field names, structural-columns v1 validation, and the hidden test seam. It may not rely
on a package-version guess, private pyowl-core extension import, schema-local ID persistence, or
the availability of encoded ingestion in an installed environment.

WP14 closes only when the external core contract and final consumer revisions are frozen, the
gate-eligible performance and installed-artifact matrices pass, and the resulting revisions and
machine-labelled evidence are appended to this report.
